# 监听
HOST=0.0.0.0
PORT=8099

# SQLite 路径（目录会自动创建）
DB_PATH=data/stats.db

# 心跳鉴权：留空则 POST /v1/heartbeat 不校验（仅建议内网/调试）
HEARTBEAT_TOKEN=

# 超过该秒数未心跳则不计入 deployments_online（默认 900 = 15 分钟）
ONLINE_TTL_SEC=900

# 拒绝时间戳与服务器相差超过该秒数的心跳（默认 300）
MAX_CLOCK_SKEW_SEC=300
